import React from 'react';

const Header = () => {
    return (
        <header className="text-center">
            <h1 className="text-4xl font-bold uppercase tracking-widest">
                Url Shortener
            </h1>
            <p className="text-xl">
                Simplify your links, one short URL at a time
            </p>
        </header>
    );
};

export default Header;
